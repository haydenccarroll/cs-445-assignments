make range loops have their own scope

cleanup (obviously)

ass (=) isnt right i dont think for initialization purposes
    , especially with arrays.

make the python tester sort the bottom of the input
