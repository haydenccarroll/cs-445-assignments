// C-F22
int a[11];

main()
{
   int b[22];           // size inited locally
   static int c[33];

   c[22];
   999;
   output(*a);
   output(*b);
   output(*c);
}

1. track size for following:
    function
    Var
    const
    id`
    Compound
    for
    (not while)

2. the following start at -2 for size. (need space for frame)
    func
    compound
    for

3. size for vars + chararr const is positive (pretty simple to calc)

4. func compound for have negative size
    frame is -2 - minimum f offset of scope
